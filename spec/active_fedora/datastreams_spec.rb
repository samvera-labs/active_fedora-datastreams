require 'spec_helper'

describe ActiveFedora::Datastreams do
  it 'has a version number' do
    expect(ActiveFedora::Datastreams::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
